using Tools.Models;

namespace Tools;

public static class Handlers
{
    public static void Remove(PredictionResult predictionResult, double threshold)
    {
    }

    public static void Move(PredictionResult predictionResult, double threshold)
    {
    }

    public static void Copy(PredictionResult predictionResult, double threshold)
    {
    }
}