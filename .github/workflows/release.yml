name: Release Build

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            rid: linux-x64
            archive: tar.gz
          - os: macos-latest
            rid: osx-arm64
            archive: tar.gz
          - os: windows-latest
            rid: win-x64
            archive: zip

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Publish AOT
        run: >
          dotnet publish src/Tools.csproj
          -c Release
          -r ${{ matrix.rid }}
          --self-contained
          -o publish

      - name: Package (tar.gz)
        if: matrix.archive == 'tar.gz'
        shell: bash
        run: |
          NAME=tools-${{ matrix.rid == 'linux-x64' && 'linux' || 'macos' }}
          mkdir "$NAME"
          cp module.json "$NAME/"
          cp publish/tool "$NAME/"
          tar -czf "$NAME.tar.gz" "$NAME"

      - name: Package (zip)
        if: matrix.archive == 'zip'
        shell: pwsh
        run: |
          $name = "tools-windows"
          New-Item -ItemType Directory -Path $name | Out-Null
          Copy-Item module.json "$name/module.json"
          Copy-Item publish/tool.exe "$name/tool.exe"
          Compress-Archive -Path $name -DestinationPath "$name.zip"

      - name: Upload artifact (tar)
        if: matrix.archive == 'tar.gz'
        uses: actions/upload-artifact@v4
        with:
          name: tools-${{ matrix.rid }}
          path: tools-*.tar.gz

      - name: Upload artifact (zip)
        if: matrix.archive == 'zip'
        uses: actions/upload-artifact@v4
        with:
          name: tools-${{ matrix.rid }}
          path: tools-windows.zip

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          files: dist/*
          generate_release_notes: true